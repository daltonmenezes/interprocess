import { styled } from 'styles'

export const LibraryVersionContainer = styled('div', {
  display: 'flex',
  fontSize: '0.85rem',
  color: '$text-support',
  maxWidth: 'fit-content',
  padding: '0.5rem',
  borderRadius: '0.5rem',
  backgroundColor: '$border-primary',
  border: '1px solid $border-primary',
})
